# Instalación de Three.js

Para que los círculos 3D funcionen, necesitas instalar Three.js:

```bash
npm install three
npm install --save-dev @types/three
```

O si usas yarn:

```bash
yarn add three
yarn add --dev @types/three
```

Después de instalar, reinicia el servidor de desarrollo:

```bash
npm run dev
```

